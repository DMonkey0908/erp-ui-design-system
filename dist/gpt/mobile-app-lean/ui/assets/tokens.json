{
  "$comment": [
    "The token file for the mobile-app pack, and the single source of truth for",
    "every colour and measurement in an app built from it.",
    "",
    "It is JSON rather than a stylesheet because the same screen gets built in",
    "SwiftUI, Compose, React Native and sometimes CSS, and a palette that lives",
    "in one of those four is a palette the other three retype. Generate the",
    "platform file from this; assets/tokens.css shows the shape for the web.",
    "",
    "TO REBRAND: change `color.accent` and `color.accentPressed`, both values",
    "of each. Nothing else moves. Never ship an accent with one value - it will",
    "disappear against one of the two themes, and dark mode is not optional",
    "here.",
    "",
    "Sizes are in pt. Treat 1pt = 1dp = 1 React Native unit = 1 Flutter logical",
    "pixel. Text sizes map to sp on Android, so they scale with the user's font",
    "setting; spacing maps to dp, so it does not."
  ],

  "color": {
    "accent": { "light": "#0a58d0", "dark": "#7aa7ff" },
    "accentPressed": { "light": "#0846a6", "dark": "#a3c2ff" },
    "onAccent": { "light": "#ffffff", "dark": "#0b0b0f" },

    "bg": { "light": "#ffffff", "dark": "#000000" },
    "bgGrouped": { "light": "#f2f2f7", "dark": "#0d0d0f" },
    "surface": { "light": "#ffffff", "dark": "#1c1c1e" },
    "surfaceRaised": { "light": "#ffffff", "dark": "#2c2c2e" },

    "text": { "light": "#0b0b0f", "dark": "#f2f2f7" },
    "textSecondary": { "light": "#5b5b66", "dark": "#a1a1aa" },
    "textTertiary": { "light": "#6e6e79", "dark": "#92929d" },

    "separator": { "light": "#d8d8de", "dark": "#38383c" },
    "borderControl": { "light": "#8b8b96", "dark": "#74747f" },
    "fillQuiet": { "light": "#f2f2f7", "dark": "#2c2c2e" },
    "pressedOverlay": { "light": "rgba(0,0,0,0.06)", "dark": "rgba(255,255,255,0.10)" },
    "scrim": { "light": "rgba(0,0,0,0.30)", "dark": "rgba(0,0,0,0.50)" },

    "success": { "light": "#1a7f45", "dark": "#48d17e" },
    "warning": { "light": "#986400", "dark": "#f0b429" },
    "danger": { "light": "#c7212f", "dark": "#ff6b6b" },
    "info": { "light": "#5b5b66", "dark": "#a1a1aa" }
  },

  "text": {
    "$comment": "Map these onto the platform's own text styles rather than setting sizes directly - that is what makes the user's font-size setting work.",
    "largeTitle": { "size": 34, "weight": 700, "ios": "largeTitle", "android": "displaySmall" },
    "title": { "size": 22, "weight": 600, "ios": "title2", "android": "headlineSmall" },
    "headline": { "size": 17, "weight": 600, "ios": "headline", "android": "titleMedium" },
    "body": { "size": 17, "weight": 400, "ios": "body", "android": "bodyLarge" },
    "subhead": { "size": 15, "weight": 400, "ios": "subheadline", "android": "bodyMedium" },
    "footnote": { "size": 13, "weight": 400, "ios": "footnote", "android": "bodySmall" },
    "caption": { "size": 12, "weight": 400, "ios": "caption1", "android": "labelSmall" }
  },

  "space": {
    "$comment": "4pt base. A value not in this set is the beginning of drift.",
    "xs": 4,
    "sm": 8,
    "md": 12,
    "screen": 16,
    "lg": 24,
    "xl": 32
  },

  "radius": {
    "control": 10,
    "card": 14,
    "sheet": 16,
    "pill": 999
  },

  "target": {
    "$comment": "core/09-input.md sets the 24pt floor; this pack raises it. Measured on the hit area, which is padding, not on the drawn icon.",
    "min": 44,
    "minAndroid": 48,
    "gap": 8,
    "rowMinHeight": 44,
    "primaryHeight": 50
  },

  "chrome": {
    "$comment": "Add the runtime safe-area inset to the bar heights. Never hardcode the inset.",
    "navBarHeight": 44,
    "tabBarHeight": 49,
    "tabLabelSize": 12
  },

  "motion": {
    "$comment": "Within core/04-motion.md's bands, at the fast end. Direction matters more than duration: a push enters from the edge it will return to.",
    "pressFeedbackMs": 80,
    "stateChangeMs": 150,
    "sheetMs": 250,
    "pushMs": 300
  }
}
